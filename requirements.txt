# Connectome access + circuit visualization (querying MaleCNS/MANC, resolving
# neurons between them, 3D rendering)
neuprint-python==0.6.3
navis==1.12.0
python-dotenv==1.2.3
numpy==2.5.3
pandas==3.0.6
matplotlib==3.11.2
plotly==7.1.0

# Running Pugliese et al.'s simulation code unmodified (external/Pugliese_cpg_2025),
# and our own steppable reimplementation of their rate model for the closed loop
jax==0.9.2
jaxlib==0.9.2
diffrax==0.7.0
hydra-core==1.3.7
omegaconf==2.3.1
sparse==0.18.0
scipy==1.18.1  # bandpass filtering, Hilbert phase, AR(1) rhythm gate (analysis/interleg_coordination.py)
scikit-learn==1.9.1
seaborn==0.13.2
pyarrow==25.0.1
h5py==3.16.0
natsort==8.4.0
psutil==7.2.2
pypdf==6.19.0  # reading their bioRxiv preprint PDF directly

# Fly body — FlyGym/NeuroMechFly v2 (github.com/NeLy-EPFL/flygym, Apache-2.0),
# NOT a hand-built or generic hobby-robot hexapod. See docs/logs/2026-09-19.md
# for why. Requires Python >=3.12,<3.15 (our venv is 3.12).
flygym==2.1.0
mujoco==3.9.0
mediapy==1.2.7
glfw==2.10.2
PyOpenGL==3.1.10

# Evolution strategy for the trained adapter (fly_robot/training/). Gradient-free
# by requirement: the connectome is never differentiated through.
cma==4.5.0

# Tests (tests/, run with `pytest tests/`) — locality, determinism, degree
# preservation and other closed-loop pre-registration gates
pytest==9.1.1
